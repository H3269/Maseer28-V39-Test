# گزارش کنترل نسخه V63 PRO RELEASE

## کنترل ساختار محتوا

- تعداد مسیر/مسئله: **۳۴**
- تعداد روز هر مسیر: **۲۸ روز کامل**
- ترتیب روزهای همه مسیرها: **۱ تا ۲۸ بدون شکاف**
- ارجاع‌های مطالعه به دو کتاب: **همه معتبر**
- شناسه‌های تکراری HTML: **وجود ندارد**
- توابع استفاده‌شده در `onclick`: **همه تعریف شده‌اند**
- JavaScript اصلی، لایه V60 و افزونه V63: **Syntax Check با Node.js موفق**
- مجموع رکوردهای روزانه بررسی‌شده: **۹۵۲ رکورد (۳۴ × ۲۸)**
- تست سناریوی مرورگر/DOM نسخه نهایی: **۲۷ از ۲۷ مورد موفق**

## سناریوهای عملکردی بررسی‌شده

- شروع برنامه → پروفایل → انتخاب مسئله → ارزیابی اولیه → داشبورد
- ثبت پروفایل مستقل و حفظ آن هنگام شروع مسیر جدید
- ویرایش پروفایل بدون پاک شدن روزهای تکمیل‌شده یا تغییر Path ID
- نمایش تمرین/اقدام قبل از فرم نتیجه و باز شدن فرم نتیجه به‌صورت Progressive Disclosure
- ثبت نتیجه روز اول و ساخته شدن رکورد روزانه
- فعال نبودن روز دوم در همان روز تقویمی
- آرشیو شدن مسیر قبلی هنگام شروع مسیر جدید
- باز شدن کتاب‌خوان Full Screen و ذخیره موقعیت مطالعه
- مسیر Bridge پیامک با `Android.openSms(...)`

## نکته مربوط به WebView

کد HTML به‌تنهایی نمی‌تواند WebViewای را که `sms:` را مسدود می‌کند مجبور به باز کردن برنامه پیامک کند. V63 در سمت وب fallback دارد، اما برای رفتار قطعی داخل اپ باید Bridge یا `shouldOverrideUrlLoading` مطابق فایل `WEBVIEW-INTEGRATION-ANDROID.md` در لایه Native فعال شود.

## قفل زمانی

قفل نسخه V63 فعال است و زمان شروع از تکمیل ارزیابی اولیه ثبت می‌شود. حالت فعلی از تقویم محلی دستگاه استفاده می‌کند؛ برای جلوگیری کامل از دستکاری ساعت دستگاه، اتصال Backend زمان معتبر توصیه می‌شود.


## کنترل اختصاصی V63

- ارزیابی اولیه: ۵ سؤال × ۵ گزینه (۰ تا ۴) = **۲۵ دکمه انتخاب** در DOM.
- ارزیابی نهایی: همان ساختار ۵ سؤال × ۵ گزینه = **۲۵ دکمه انتخاب**.
- شناسه‌های HTML تکراری: **۰ مورد**.
- پاسخ‌های پنج‌گانه در `assessmentData.answers` و `final.answers` ذخیره و توسط Sanitizer V63 حفظ می‌شوند.
- شاخص شدت گزارش از چهار سؤال اول ساخته می‌شود؛ سؤال پنجم (انگیزه تغییر) مستقل ذخیره می‌شود.
- جهت طیف نوار شدت اصلاح شد: **سبز در سمت مقدار ۰ → زرد → قرمز در سمت مقدار ۱۰**.
- Syntax Check نهایی JavaScript با Node.js: **موفق**.
- تست رندر Headless در این محیط به‌دلیل محدودیت دسترسی مرورگر به localhost/file توسط محیط اجرایی قابل انجام نبود؛ کنترل ساختاری، Syntax و منطق ذخیره‌سازی انجام شده است.

## اصلاحات V63
- هدر چسبان در اسکرول دیگر بین حالت عادی/Compact تغییر اندازه نمی‌دهد؛ listener مبتنی بر جهت اسکرول حذف شد تا پرش WebView از بین برود.
- اسلایدر شدت به کنترل native range تبدیل شد تا Drag/Touch در Android WebView پایدار باشد.
- جهت اسلایدر RTL است: ۰ سمت راست و سبز، ۱۰ سمت چپ و قرمز؛ حباب مقدار نیز با همین جهت همگام است.
- عبارت نمایشی «نسخه انتشار حرفه‌ای ...» از Footer حذف شد.

## اصلاحات V63
- `APP_CONFIG.TEST_MODE = true` برای باز بودن روزها در تست.
- پیام محدودیت زمانی روز بعد از مسیر `openDay()` حذف شد و در هیچ حالت از آن مسیر Validation Modal نمایش داده نمی‌شود.
- Border/Outline ورودی Native اسلایدر شدت و Border ترک اسلایدر حذف شد؛ فقط خط گرادیانی و Thumb باقی می‌ماند.
- بررسی Syntax جاوااسکریپت و Smoke Load در Chromium انجام شد.
