# راهنمای ویرایش کتاب‌خوان V72

## فایل اصلی
برای ویرایش رفتار کتاب‌خوان فقط این فایل را باز کنید:

`index.html`

پیشنهاد: با **Visual Studio Code** باز کنید و با `Ctrl+F` نام تابع/شناسه‌های زیر را جست‌وجو کنید. به شماره خط وابسته نشوید، چون با هر ویرایش جابه‌جا می‌شود.

## 1) جهت Swipe
جست‌وجو کنید:

`function v65BindReaderGestures()`

داخل تابع، بخش `finish` رفتار Swipe را تعیین می‌کند. نسخه فعلی:

```js
if(dx>0) bookNext(); else bookPrev();
```

- `dx > 0` یعنی حرکت انگشت **از چپ به راست** → صفحه بعد
- `dx < 0` یعنی حرکت انگشت **از راست به چپ** → صفحه قبل

## 2) کلیدهای کیبورد
جست‌وجو کنید:

`function v69ReaderKeyboard(e)`

و در انتهای فایل نیز اسکریپت:

`id="v22-persian-nav-hardening"`

در نسخه فعلی:

```js
ArrowLeft  -> bookNext()
ArrowRight -> bookPrev()
```

نکته مهم: در فایل فعلی چند نسل کد V65/V67/V69/V70/V72 و V22/V23 روی هم قرار گرفته‌اند. **کدی که پایین‌تر در فایل است می‌تواند کد بالاتر را override کند.** اگر رفتاری را عوض می‌کنید، حتماً آخرین override را هم بررسی کنید.

## 3) دکمه‌های فلش پایین
### جهت فلش‌های پایین
- فلش **سمت چپ**: صفحه بعد
- فلش **سمت راست**: صفحه قبل

جست‌وجو کنید:

`class="readerBottombar"`

دو دکمه زیر به نوار پایین اضافه شده‌اند:

```html
<button class="readerPageArrow readerPrevArrow" onclick="bookPrev()">←</button>
<button class="readerPageArrow readerNextArrow" onclick="bookNext()">→</button>
```

استایلشان در انتهای فایل با شناسه زیر است:

`v72-editable-nav-arrows`

## 4) نوار صفحات RTL
جست‌وجو کنید:

`readerPageRange`

قاعده اصلی:

```css
#bookReader .readerPageRange { direction: rtl !important; }
```

صفحه 1 در سمت راست قرار می‌گیرد.

## 5) موتور صفحه‌بندی — مهم‌ترین بخش
جست‌وجو کنید:

`function v72LayoutBook(b,keepSource=1)`

این تابع کتاب را به صفحات 16 سطری تبدیل می‌کند. سه تابع مهم داخل/کنار آن:

- `v67Wrap()` → شکستن متن به سطرها
- `v72SentenceSegments()` → تشخیص مرز جمله
- `v72LayoutBook()` → قرار دادن سطرها داخل صفحات 16 سطری

### چرا صفحه‌بندی گاهی طبیعی نیست؟
نسخه فعلی برای محاسبه عرض سطر از `canvas.measureText()` استفاده می‌کند، ولی متن نهایی داخل Telegram WebView با DOM واقعی، فونت واقعی دستگاه و metric متفاوت رندر می‌شود. بنابراین تخمین Canvas همیشه با آنچه کاربر می‌بیند دقیقاً یکسان نیست.

### راه حل پایدار
اگر صفحه‌بندی واقعاً شبیه کتاب می‌خواهید، موتور باید با **DOM Measurement واقعی** بازنویسی شود:
1. یک صفحه مخفی با همان width/font/line-height ساخته شود.
2. پاراگراف‌ها یکی‌یکی داخل آن قرار بگیرند.
3. با `scrollHeight <= clientHeight` بررسی شود که جا می‌شوند یا نه.
4. اگر پاراگراف جا نشد، اول در مرز جمله و بعد در مرز کلمه تقسیم شود.
5. فصل/پیوست همیشه قبل از ورود متن `flush()` شود و صفحه عنوان مستقل بگیرد.

این روش از «تخمین تعداد کاراکتر/عرض Canvas» مطمئن‌تر است و روی Telegram Mini App و گوشی‌های مختلف نتیجه طبیعی‌تری می‌دهد.

## 6) تعداد خطوط
جست‌وجو کنید:

`const READER_LINES_PER_PAGE=16;`

اگر فقط تعداد خطوط را تغییر می‌دهید، همین عدد را عوض کنید. ولی برای حفظ کیفیت، بعد از تغییر باید `v65ReaderMetrics()` و صفحه‌بندی دوباره تست شوند.

## پیشنهاد برای توسعه بعدی
بهتر است در نسخه بعدی موتور کتاب‌خوان از `index.html` جدا شود:

- `reader.css` → فقط ظاهر کتاب‌خوان
- `reader.js` → Swipe/Keyboard/دکمه‌ها
- `paginator.js` → فقط صفحه‌بندی
- `index.html` → فقط UI و داده‌ها

این کار جلوی override شدن نسخه‌های قدیمی روی نسخه جدید را می‌گیرد و عیب‌یابی بسیار ساده‌تر می‌شود.
